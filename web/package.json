{
  "name": "ac-remote-web",
  "version": "0.0.0",
  "private": true,
  "description": "No dependencies on purpose. The site is pre-built with nix (elm 0.19.2, which is nixpkgs-only) into web/dist and served by Cloudflare Pages directly. This manifest exists only so Cloudflare's mandatory `npm clean-install` step succeeds; the build itself runs in the build-web GitHub Action."
}
